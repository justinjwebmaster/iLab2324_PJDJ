<!-- Icône du bot dans un rond -->
<div class="absolute -top-8 left-2 bg-blue-500 p-2 rounded-full">
  <!-- Insérez ici votre icône -->
</div>

<div class="pl-16">
  <!-- Nom du bot -->
  <h4 class="font-semibold mb-2">No-ID</h4>

  <!-- Bulle de message -->
  <div class="bg-blue-500 text-white p-4 rounded-xl">
    {{ message }}
  </div>
</div>

